-- Объединение источников подрядчика для ввода расходов: несколько источников
-- с ОДИНАКОВЫМ типом оплаты можно объединить в одну строку на странице
-- "Ввод расходов" (вместо ввода расхода по каждому источнику отдельно).
-- expense_group_id одинаковый у всех участников одной группы — просто
-- сгенерированный uuid, отдельная таблица групп не нужна (у группы нет своих
-- полей, кроме списка участников).
alter table sources add column if not exists expense_group_id uuid;
